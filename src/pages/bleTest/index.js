import React from 'react';
import {View, Text, TouchableOpacity} from 'react-native';

import Styles from './styles';

const BleTest = ({navigation}) => {
  return <View style={Styles.mainContainer} />;
};

export default BleTest;
